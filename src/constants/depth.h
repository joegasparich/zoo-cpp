#pragma once

namespace DEPTH {
    const float GROUND = -1;
    const float WATER = -2;
    const float OVERLAY = -4;
    const float Y_SORTING = -5;
    const float DEBUG = -8;
    const float UI = -9;
}
